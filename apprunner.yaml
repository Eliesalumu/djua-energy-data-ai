version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building DJUA Energy API container"
run:
  command: uvicorn apps.api.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
    env: PORT
  env:
    - name: PYTHONPATH
      value: /app/src
